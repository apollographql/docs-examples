type Query {
  record(id:ID!): Record
  records: [Record]
}

type Mutation {
  createRecord(name: String!, position: String, level: Int): Record
}

type Record {
  id: ID
  name: String
  position: String
  level: Int
}